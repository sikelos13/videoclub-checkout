import { Movie } from "../api/types/Movie";

export const getUpdatedItemsListOnDelete = (id: string, list: Movie[]) => {
    // return list.filter((player: Player) => {
    //     return id !== player.id;
    // });

}