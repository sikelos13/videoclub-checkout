import { Movie } from "../api/types/Movie";

export const getUpdatedItemsListOnEdit = (form: any, list: Movie[]) => {
    // return list.map((player: Player) => {
    //     if (form.id === player.id) {
    //         return {
    //             ...player,
    //             name: form.name
    //         }
    //     }
    //     return player;
    // });
}